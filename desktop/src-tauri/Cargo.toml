[package]
name = "gigaam-desktop"
version = "2.0.5"
description = "Native desktop shell for GigaAMGUI"
authors = ["GigaAMGUI"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

[features]
custom-protocol = ["tauri/custom-protocol"]
