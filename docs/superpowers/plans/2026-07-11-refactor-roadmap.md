# Рефакторинг монолитов — дорожная карта планов

Реализация спеки `docs/superpowers/specs/2026-07-11-monolith-refactor-design.md`.
Подход C, инкрементально, поведение поверхностей строго 1:1.

Каждая фаза = отдельный план, дающий рабочий тестируемый результат. Планы Фаз 2–5
детализируются точечно перед выполнением, т.к. их шаги зависят от результата Фазы 1
(имена сервисов, сигнатуры). Детализировать раньше — значит писать догадки (= плейсхолдеры).

| Фаза | План | Статус | Риск | Итог фазы |
|---|---|---|---|---|
| 0 | `2026-07-11-phase-0-repo-hygiene.md` | ✅ написан | низкий | чистое дерево, зелёный baseline |
| 1 | `2026-07-11-phase-1-service-layer.md` | ✅ сделано | низк-средн | `src/services/` без дублей; монолиты делегируют |
| 2 | (по факту) | ✅ сделано | низкий | форматтеры вынесены из `processor.py` в `core/formatters.py` |
| 3 | (см. ниже) | ✅ сделано (сужено) | средний | роуты делегируют в сервисы; package-split сознательно не делаем |
| 4 | (по факту) | ✅ сделано | высокий | `app_qt.py` god-class → 4 mixin-модуля (−30%: 3735→2616) |
| 5 | (по факту) | ✅ сделано | низк-средн | интерактивный выбор → `src/cli_support/`, entry-файл сохранён |

### Фаза 5 — что сделано
`src/cli_support/interactive.py` — `get_supported_files`/`select_files_interactive`/`select_output_directory`
вынесены из `cli.py` (−135 строк), зависимости `console`/`style` передаются явно. `cli.py` остаётся
entry-файлом (`python cli.py` работает). Найден и устранён латентный баг: алиас импорта `interactive`
конфликтовал бы с параметром `--interactive` в `main()` → импорт назван `cli_interactive`.

## Итог рефакторинга

| Файл | Было | Стало | Δ |
|---|---|---|---|
| `src/gui/app_qt.py` | 3834 | ~2616 | −32% (4 mixin-а) |
| `cli.py` | 677 | ~540 | −20% |
| `src/core/processor.py` | 634 | ~500 | форматтеры вынесены |

**Новые модули:** `src/services/{file_policy,health,llm_service,task_store,transcription_service}.py`,
`src/core/formatters.py`, `src/gui/{llm,llm_ui,download,theme}_mixin.py`, `src/cli_support/interactive.py`.

**Устранённое дублирование:** LLM-диспетчер (был в app_qt+web), task-schema/health/file-validation
(были в api+web), сборка процессора (была в 4 местах) — теперь в едином `src/services/`.

**Тесты:** 177 → 209 (+32 характеризующих). Падений: 2 (оба pre-existing, GUI-layout, не связаны с рефакторингом).
**Ruff:** 48 → 27 ошибок в репо (чистка). Все новые модули ruff-чисты.
**Поведение:** строго 1:1; расхождения поверхностей сохранены флагами (`strict_empty_cli`, glob-vs-set,
нормализация провайдера). Entrypoints (`python api.py`, `python cli.py`, `web.web_app:app`) не изменены.

### Фаза 4 — что сделано
`GigaTranscriberQtApp` разбит на 11 mixin-модулей (класс сохраняет идентичность → GUI-тесты и
entrypoint не меняются). **Все GUI-модули ≤600 строк** (было: один файл 3834):

| Модуль | Концерн | строк |
|---|---|---|
| `app_qt.py` | ядро: класс, `__init__`, палитры, запуск, instance-lock | 552 |
| `ui_build_mixin.py` | `_init_ui`, меню, `_create_*_group` | 582 |
| `llm_ui_mixin.py` | построение вкладки LLM | 498 |
| `llm_mixin.py` | LLM-обработка, панель файлов, промпты | 454 |
| `processing_mixin.py` | запуск/прогресс обработки | 426 |
| `theme_mixin.py` | `_apply_theme` (палитра + QSS) | 403 |
| `files_mixin.py` | список файлов, drag-drop, HF-token диалог | 366 |
| `i18n_mixin.py` | язык/переводы | 281 |
| `settings_mixin.py` | restore/save настроек, geometry, лог | 213 |
| `style_mixin.py` | масштаб UI, шрифты, метрики | 100 |
| `download_mixin.py` | загрузка медиа | 93 |

Новые тесты: `tests/test_gui_llm_mixin.py`; GUI source-scrape тесты читают весь пакет `src/gui`.
Функциональный smoke (24 проверки) подтвердил работу методов каждого mixin-а в рантайме.

**Латентные баги, пойманные при декомпозиции и исправленные:**
- `_is_headless` терял `@staticmethod` (декоратор утягивался в соседний метод) → окно падало при
  `_restore_geometry`. Экстрактор исправлен, декоратор восстановлен.
- `_show_about` ошибочно становился `@staticmethod` → упал бы при клике «О программе».
- web LLM-делегация без `raise ... from` (косметика трейсбека).

**Гейт между фазами:** `ruff check` изменённых файлов + `pytest` зелёный (не хуже baseline: 2 pre-existing GUI-падения) + smoke изменённых поверхностей.

## Решение по Фазе 3 (сужение объёма — обосновано)

Полный router/package-split `api.py`/`web_app.py` **сознательно не выполняется**, потому что он нарушил бы строгое 1:1 и контракт тестов:

1. **Entrypoints.** `api.py` запускается как `python api.py` (README), `web_app` — как `web.web_app:app` (Dockerfile). Превращение `api.py` в пакет `api/` ломает `python api.py` — это задокументированное поведение (нарушение 1:1).
2. **Контракт тестов.** Тесты патчат 30+ атрибутов модулей: `api.VALID_API_KEY_HASHES`, `api.verify_api_key`, `api._hash_key`, `web_app.tasks_storage` (18×), `web_app.log_queues`, `web_app._restore_*`, `web_app.save_json_atomic` и т.д. `verify_api_key` обязан читать патчимый `api.VALID_API_KEY_HASHES` из namespace `api`; restore-функции держат общий мутабельный реестр задач. Вынос в подмодули сломал бы патчи без переписывания как surface-кода, так и тестов — при реальном риске регресса auth/persistence.
3. **Цель фазы уже достигнута.** Задача Фазы 3 — «тонкие роуты над сервис-слоем». После Фазы 1 роуты `api.py`/`web_app.py` уже делегируют валидацию, health, LLM, схему задач и сборку процессора в `src/services/`. Дальнейшее дробление — движение ради движения (риск option C, который мы договорились не брать).

Итог: backend декомпозирован настолько, насколько безопасно при строгом 1:1. Усилие декомпозиции перенесено в Фазу 4 (GUI), где оно безопасно (класс сохраняет идентичность через mixin-и) и высокоценно.

## Обнаруженные дивергенции (зафиксировать тестами, не «чинить» молча)

Обнаружены при чтении кода на этапе планирования; каждая — риск нарушить 1:1 при наивном
объединении дублей:

1. **`is_supported_format`** — `api.py` использует `SUPPORTED_FORMATS[1].split()` со срезом `*`;
   `web_app.py` — членство в `MEDIA_EXTENSIONS`. Разные источники допустимых расширений.
   → `file_policy` параметризуется источником; каждая поверхность сохраняет свой.
2. **LLM пустой ответ** — GUI (`app_qt.py`) бросает «… вернул пустой ответ» для Claude Code/Codex;
   `web_app.py` не проверяет (возвращает пустую строку). → сервис поддерживает флаг
   `strict_empty`; GUI=True, web=False (сохранение 1:1). Расхождение помечено как латентный
   баг web — вынести пользователю отдельно, не менять в этом рефакторинге.
3. **Провайдер «Other/Другое»** — GUI сверяет `_normalize_llm_provider(...)=="Other"`;
   web — `provider=="Другое"`. → нормализацию имени провайдера инкапсулировать в сервисе,
   адаптеры передают своё сырое значение.
4. **Текст ошибки неизвестного провайдера** различается (GUI локализует через `_t`).
   → сервис бросает нейтральный ключ/исключение; текст формирует адаптер.
